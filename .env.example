# Copy to .env and fill in. Never commit .env.
# This file grows as milestones land; M0 has no runtime configuration yet.

# --- Application ---
APP_ENV=local          # local | dev | staging | production
LOG_LEVEL=info

# --- Added in later milestones (do not set yet) ---
# M1: DATABASE_URL=postgresql+asyncpg://...
# M1: REDIS_URL=redis://localhost:6379/0
# M2: SUPABASE_URL=..., SUPABASE_JWT_SECRET=...
# M6: ANTHROPIC_API_KEY=...   (BYOK; resolved via SecretStore, never logged)
